{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "company",
    "studio_archetype",
    "operating_model",
    "studio_personality",
    "creative_dna",
    "visual_dna",
    "decision_maker",
    "buyer_personality",
    "most_distinctive_insight",
    "colorkaar_angle",
    "conversation_starter",
    "avoid",
    "why_this_lead",
    "outreach_priority",
    "evidence_strength",
    "reasoning"
  ],
  "properties": {
    "company": {
      "type": "string",
      "description": "The company name as given in the input."
    },
    "studio_archetype": {
      "type": "string",
      "description": "One short phrase: founder-led boutique, DP-led, production company, agency, huge production company, or unknown. Prefer \"production company\" over \"unknown\" whenever the evidence (including the company name itself) supports it - see the reasoning ladder."
    },
    "operating_model": {
      "type": "string",
      "description": "ONE short structural fact not already implied by studio_archetype - e.g. \"Owner-operated\", \"In-house production and post\", \"Works with multiple external clients\". Do not restate studio_archetype here. UNKNOWN only if the reasoning ladder is fully exhausted."
    },
    "studio_personality": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["Independent", "Boutique", "Hands-on", "Relationship-driven", "Structured", "Collaborative", "Scalable", "Experimental", "Image-first"]
      },
      "description": "1-3 tags from the closed vocabulary describing how you'd work with them day to day. Empty array only if the reasoning ladder is fully exhausted."
    },
    "creative_dna": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["Luxury", "Fashion", "Beauty", "Food", "Travel", "Outdoor", "Sports", "Music", "Narrative", "Documentary", "Comedy", "Automotive", "Architecture", "Corporate"]
      },
      "description": "1-3 tags from the closed vocabulary answering: what kind of creative work excites them? A genuine creative specialty, not a restatement of what their clients happen to sell. Empty array if none genuinely fit - this one stays conservative, since a wrong genre tag is worse than none."
    },
    "visual_dna": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["Naturalistic", "Graphic", "Bold", "Filmic", "Minimal", "Textured", "Controlled", "Organic", "Raw", "Elegant"]
      },
      "description": "1-3 tags from the closed vocabulary describing the actual look - the vocabulary a colorist would use to start a grade. Empty array if none genuinely fit - this one stays conservative, since a wrong look tag is worse than none."
    },
    "decision_maker": {
      "type": "string",
      "description": "Name and/or title only, short. e.g. \"Founder (Mara)\". UNKNOWN only if no name or title at all is given."
    },
    "buyer_personality": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["Relationship-driven", "Craft-first", "Process-driven", "Schedule-driven", "Quality-first", "Founder-led", "Collaborative", "Risk-averse"]
      },
      "description": "1-3 tags from the closed vocabulary on how they evaluate and decide. Empty array only if the reasoning ladder is fully exhausted."
    },
    "most_distinctive_insight": {
      "type": "string",
      "description": "The single most distinctive thing about who this company is - not necessarily something rare or unusual, just the strongest available insight. Follow the reasoning ladder: a unique detail beats a meaningful one, a meaningful one beats the plain business model, and the plain business model beats UNKNOWN. UNKNOWN is a last resort, not a default. A few words, not a sentence. Never a generic compliment (\"award-winning\", \"passionate\")."
    },
    "colorkaar_angle": {
      "type": "string",
      "description": "A short punchy phrase, roughly 3-8 words, on why Colorkaar specifically matters here. Not a sentence that explains itself with an em-dash. Not an email."
    },
    "conversation_starter": {
      "type": "string",
      "description": "A specific topic to open with, a few words, not a compliment."
    },
    "avoid": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Short tags (1-3 words each) for topics/tones that would misread the room. e.g. [\"Pricing\", \"Awards\", \"Volume framing\"]. Not sentences. Free text - situational, not a closed vocabulary."
    },
    "why_this_lead": {
      "type": "array",
      "items": { "type": "string" },
      "description": "3-5 short bullets synthesizing the case for outreach - the gut-check a producer reads before deciding whether to write at all. Can reuse tags from other fields plus specific facts. e.g. [\"Founder-led\", \"Image-first\", \"No internal grading\", \"Relationship-driven\"]."
    },
    "outreach_priority": {
      "type": "string",
      "enum": ["Immediate", "High", "Medium", "Low"],
      "description": "Your proposed priority tier. Recomputed deterministically downstream from studio_archetype and evidence_strength - reason honestly."
    },
    "evidence_strength": {
      "type": "string",
      "enum": ["Strong", "Moderate", "Weak"],
      "description": "How much of this profile rests on facts stated directly in the evidence (including the company name) versus reasonable inference. NOT a measure of how interesting or complete the profile is - a Weak-evidence profile can and should still have real values in every field, just clearly built on inference rather than direct statement. Strong: most fields come from facts stated directly. Moderate: a real mix of stated fact and inference. Weak: almost everything is inferred from thin signal (a name, a title, a one-line description)."
    },
    "reasoning": {
      "type": "string",
      "description": "Debug only, dropped before export. Briefly separate what was FACT (stated directly), what was INFERENCE (reasoned from that fact), and what was the resulting INSIGHT - e.g. \"Fact: description says 'production company', 'directors' cuts'. Inference: works across multiple client types, not a single-brand in-house team. Insight: directors'-cuts specialist.\""
    }
  }
}
